function percentIncrease(originalNum, newNum) {
    let substractAnswer = newNum - originalNum;
    let fractionAnswer = substractAnswer / originalNum
    let multiply100 = fractionAnswer * 100;
    return multiply100 + "%";
}
 console.log(percentIncrease());

 /*MADE BY RyanGitHub101*/
